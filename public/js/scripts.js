// Inicializa las animaciones AOS (Animation On Scroll)
AOS.init({
    duration: 1000,  // Duración de la animación
    easing: 'ease-in-out',  // Efecto de la animación
    once: true,  // La animación solo ocurre una vez
  });
  